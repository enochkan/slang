#include <vector>
#include <string>

struct ASTNode {
    std::string type;
    std::string value;
};

ASTNode parse(const std::vector<Token>& tokens) {
    // Parsing logic here
    // Same as before
}
