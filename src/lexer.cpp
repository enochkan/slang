#include <iostream>
#include <regex>
#include <vector>
#include <string>

struct Token {
    std::string type;
    std::string value;
};

std::vector<Token> tokenize(const std::string& code) {
    // Tokenizing logic here
    // Same as before
}
